# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environment
venv/

# PyInstaller intermediate build files only
# dist/ is intentionally committed so the binary is downloadable
build/
*.spec.bak

# Game assets — paid assets, not distributed with source
# Anyone cloning this repo must provide their own newassets/ directory
newassets/

# Save files — user-specific, regenerated at runtime
saves/

# IDE / OS
.vscode/
.idea/
*.DS_Store
Thumbs.db
